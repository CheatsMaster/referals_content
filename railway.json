{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "chmod +x start_with_backup.sh && ./start_with_backup.sh",
    "restartPolicyType": "ALWAYS"
  }
}
